{
  "name": "@oshuster/pdf-generator",
  "version": "1.0.1",
  "description": "A library for generating PDFs using Playwright",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npx tsc",
    "start": "node dist/index.js",
    "prepare": "pnpm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/oshuster/pdf-generator.git"
  },
  "keywords": [
    "pdf",
    "playwright",
    "pdf-generation"
  ],
  "author": "oshuster",
  "license": "MIT",
  "private": false,
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  },
  "dependencies": {
    "playwright": "^1.51.1"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.2"
  }
}
